import java.util.stream.Stream

class Node(val index1: Int, val index2: Int) {
/*   fun adjacents(limit1: Int, limit2: Int): Stream<Node>{
      val stream = Stream.of(){

      }
      if (index1>0){

      }
   }*/
}